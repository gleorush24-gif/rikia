[build]
  base = "mobile"
  publish = "build/web"
  command = "git clone https://github.com/flutter/flutter.git --depth 1 -b stable && export PATH=$PWD/flutter/bin:$PATH && flutter config --no-analytics && flutter pub get && flutter build web --release"

[build.environment]
  FLUTTER_VERSION = "stable"
